---
# defaults file for makeiso
centos_net_iso: /root/CentOS-7-aarch64-NetInstall.iso
centos_ful_iso: /root/CentOS-7-aarch64-Everything.iso
centos_iso: "{{ centos_net_iso }}"
Label: CentOS-7.4-aarch64
OS: CentOS #CentOS Redhat Ubuntu Deepin
REPO: http # cdrom nfs http

topdir: /run/iso
pkgsdir: "{{ topdir }}/pkgs"
builddir: "{{ topdir }}/build"
outputdir: "{{ topdir }}/output"
inputdir: "{{ topdir }}/input"
